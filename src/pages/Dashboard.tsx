import styles from "./Dashboard.module.css";

function Dashboard() {
  return (
    <div className={styles.horizontal}>
      <h1 className={`${styles.heading} ${styles.h1}`}>Dashboard</h1>
      <p>TEST</p>
    </div>
  );
}

export default Dashboard;
